pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout repository'
            }
        }

        stage('Build') {
            steps {
                echo 'Build step'
            }
        }

        stage('Test') {
            steps {
                echo 'Test step'
            }
        }
    }
}